import { DefaultTheme } from 'react-native-paper';

const theme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: '#129990',
        accent: '#03DAC6',
        background: '#f6f6f6',
        googleRed: '#DB4437'
    },
    roundness: 8,
};

export default theme;